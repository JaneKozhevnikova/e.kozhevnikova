Domain Analysis Testing
1.Разбиение на классы эквивалентности. Определение границ каждого класса. Подбор тестов для входящих, 
не входящих и граничных значений.Тестирование нескольких переменных для каждого рабиения одновременно. 
2.Строка поиска, поля логин/пароль в авторизации, календарь.
3.Не подходит для тестирования числовых значиний типа double, float. 

Cause Effect Testing
1. Отражает требования логической связи между входными и выходными значениями. 
2. Проверка авторизации, написание комментариев на сайте, отправка сообщений.
3. Не используется там, где входные значения не зависят от выходных. 
